# E — seleccionar/intentar/lanzar

- `e_lanzar_fuera_de_intentar.jasb`: debe fallar en compilacion (error semantico).
- `e_seleccionar_sin_fin.jasb`: debe fallar por cierre faltante (`fin_seleccionar`).

