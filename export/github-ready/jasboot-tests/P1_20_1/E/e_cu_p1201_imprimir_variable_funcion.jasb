# E: `imprimir` directo sobre variable `funcion` (sin llamada g(...)).

funcion id(entero x) retorna entero
    retornar x
fin_funcion

principal
    funcion h
    h = id
    imprimir h
fin_principal
