# E: la condicion de `mientras ... hacer` no puede ser el nombre solo de una funcion global (mismo rechazo que en `si`).

funcion duplicar(entero x) retorna entero
    retornar x * 2
fin_funcion

principal
    mientras duplicar hacer
        romper
    fin_mientras
fin_principal
