# E: asignar a `flotante` el nombre de una funcion global (`duplicar`) sin contexto `funcion`.
#     El compilador rechaza usar el identificador de funcion global como valor escalar.

funcion duplicar(entero x) retorna entero
    retornar x * 2
fin_funcion

principal
    flotante z = duplicar
fin_principal
