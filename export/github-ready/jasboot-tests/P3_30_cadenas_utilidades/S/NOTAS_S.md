# P3_30 — Notas S (estrés)

| Archivo | Objetivo |
|---------|----------|
| `s_estres_p30_concat_dividir.jasb` | ~3000 concatenaciones `",a"` sobre una cadena base y `dividir_texto(..., ",")` → **3001** tokens. Comprueba cadena grande + tabla de lista en VM/JMN. |

Salida esperada:

```text
3001

```

Ejemplo:

```text
bin\jb.cmd tests\P3_30_cadenas_utilidades\S\s_estres_p30_concat_dividir.jasb
```

Puede tardar varios segundos según máquina.
