# P2_24_trig_exp_log — notas S

## `s_estres_p24_trig_exp_mil.jasb`

Bucle **80 000** iteraciones: en cada una `sin`, `cos` y `exp` sobre un angulo incremental; acumula **suma** (flotante).

**Salida esperada** (primera linea numerica, luego vacia): **`8.0766`** (float32 con la VM actual; si difiere, actualizar comentario en el `.jasb` y esta nota).

Ejecutar: `.\bin\jb.cmd tests\P2_24_trig_exp_log\S\s_estres_p24_trig_exp_mil.jasb`
