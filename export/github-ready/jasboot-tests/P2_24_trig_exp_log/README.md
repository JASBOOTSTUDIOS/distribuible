# P2_24_trig_exp_log — `sin`, `cos`, `tan`, `atan2` / `arcotangente2`, `exp`, `log`, `log10`

Ítem de catálogo: **P2 · 24** en [CHECKLIST_INSTRUCCIONES_JASBOOT_CATALOGO_TESTS.md](../../docs/PROYECTO/CHECKLIST_INSTRUCCIONES_JASBOOT_CATALOGO_TESTS.md).

Ángulos en **radianes**. `get_expression_type` en `codegen.c` debe inferir **flotante** para estas llamadas (evita `imprimir` como entero). VM: `OP_SIN`, `OP_COS`, `OP_TAN`, `OP_ATAN2`, `OP_EXP`, `OP_LOG`, `OP_LOG10` (`vm.c`).

| Carpeta | Contenido |
|---------|-----------|
| **R/** | Tablas de valores de referencia. Ver `R/NOTAS_R.md`. |
| **E/** | Aridad y llamadas sin argumentos. Ver `E/NOTAS_E.md`. |
| **S/** | Muchas evaluaciones trig/exp en bucle (checksum). Ver `S/NOTAS_S.md`. |
