# S — Estrés comparaciones (P05)

- **`s_produccion_matriz_comparaciones.jasb`**: bucle largo donde cada vuelta recorre **== != < > <= >=** y expresiones con **`y` / `o` / `no`** (patrón reglas + flags). Última salida de referencia: **83767** (regresión si cambia con el mismo `jbc`).

## Integración

`tests/P0_estres_produccion_P01_P09_integracion/S/s_repeticion_todas_producciones_P01_P09.ps1`.
