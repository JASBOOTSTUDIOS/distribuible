# P2_25_bits_unsigned — notas S

## `s_estres_p25_shift_medio_millon.jasb`

**500 000** iteraciones: `bit_shl`, `>>` y `<<` sobre indices; acumulador **entero**.

Salida esperada: **`31343500030`** (recalibrar con `jb.cmd` si cambia la VM).
