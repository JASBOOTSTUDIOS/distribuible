# P2_29_mapa_crear_poner_obtener — notas R

| Archivo | Esperado (`jasboot-ir-vm` tras `jbc`) |
|---------|----------------------------------------|
| `01_r_p29_mapa_basico_tres_claves.jasb` | `10`, vacía, `20`, vacía, `30`, vacía, `200`, vacía, `ok`, vacía, texto error (clave inexistente), vacía. |
| `02_r_p29_corchetes_mapa.jasb` | `7`, vacía, `7`, vacía. |
| `03_r_p29_impresion_de_mapa.jasb` | Una línea `[mapa id=… tamano=1]` (id numérico del handle). |
| `04_r_p29_mapa_de_funciones.jasb` | `14`, vacía, `21`, vacía, `0`, vacía — valores del mapa son funciones; `mapa_obtener` → `funcion` y llamada indirecta. |
