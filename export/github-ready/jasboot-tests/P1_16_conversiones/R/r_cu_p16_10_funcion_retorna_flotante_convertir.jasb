# 10 — Funcion retorna flotante vía convertir_flotante + decimal para imprimir (P1_15).
# Salida: 3.1416

funcion lee_pi() retorna flotante
    retornar convertir_flotante("3.14159")
fin_funcion

principal
    flotante p = lee_pi()
    imprimir(decimal(p, 4))
fin_principal
