# 4 — Funcion retorna entero desde parametro texto (funcion + retornar + convertir).
# Salida: 55

funcion lee_entero(texto cadena) retorna entero
    retornar convertir_entero(cadena)
fin_funcion

principal
    entero n = lee_entero("55")
    imprimir(n)
fin_principal
