# P1_11 — `funcion` / `retorna` / `retornar`

Cobertura para funciones de usuario, tipo de retorno declarado y sentencia `retornar`.

## Contenido

| Ruta | Rol |
|------|-----|
| `E/` | Errores de estructura y uso invalido de `retornar`. |
| `R/` | Casos validos de llamada, retorno y recursion funcional. |
| `S/` | Estres de recursion/profundidad y muchas invocaciones. |

