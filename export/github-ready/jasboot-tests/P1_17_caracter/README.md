# P1 · 17 — `caracter`, extracción y códigos

Cubre el checklist **P1 | 17**: tipo `caracter` (literal comilla simple `'x'`), `str_extraer_caracter`, `codigo_caracter`, `caracter_a_texto`, `byte_a_caracter`, `caracter_a_byte`.

- **R/** rendimiento y recorrido de cadena.
- **E/** errores de compilación (llamadas de sistema sin argumentos).

La entrada por teclado filtrada y `entrada_flotante` están en `tests/P1_17_entrada_filtrada/` (tema aparte).
