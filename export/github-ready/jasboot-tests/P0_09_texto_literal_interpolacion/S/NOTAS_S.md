# S — Estrés

Script `s_repeticion_compile_run.ps1`: repite compilación y ejecución (`jbc -e`) del programa R de interpolación para comprobar estabilidad del compilador.

## Producción (P09)

- **`s_produccion_interp_multihueco.jasb`**: muchas líneas con varias interpolaciones por literal (`seq`, `etiqueta`, expresión) — patrón logs correlacionados.
- Integración completa: `tests/P0_estres_produccion_P01_P09_integracion/S/s_integracion_catalogo_P01_P09.jasb`.
